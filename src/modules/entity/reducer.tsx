const initialState = {
  posts: {
    queries: {},
    ids: {
      someid: {
        lastUpdated: Date.now(),
        fetching: false,
        data: {}
      }
    }
  }
};

export default () => {};
